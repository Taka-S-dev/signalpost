{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the approval panel",
  "windows": ["panel"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
